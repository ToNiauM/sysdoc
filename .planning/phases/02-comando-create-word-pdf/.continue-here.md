---
context: phase
phase: 02-comando-create-word-pdf
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-05-12T00:00:00-03:00
---

<current_state>
Phase 2 is paused in an in-progress, partially formalized state.

The implemented product direction has shifted from the original narrow "TR from ETP" plan toward a more general document workflow:
- project structure: `documentos/`, `referencias/`, `output/`, `.sysdoc/`
- `prepare` extracts supported files from both `documentos/` and `referencias/`
- `render`/`publish`/`deploy` use `output/`
- `create` generates DOCX from a JSON plus a DOCX template

Commit `ca4db6f` captured the first wave of that restructuring. After that, additional Phase 2 work exists in the working tree and is not committed yet. The current test suite passes:

```bash
python -m pytest tests/ -v
# 63 passed
```

`$gsd-add-tests 2` was attempted and correctly stopped because Phase 2 has no `02-SUMMARY.md`.
</current_state>

<completed_work>

Completed:
- Generalized project structure to `documentos/`, `referencias/`, and `output/`.
- Updated cache layout to preserve origin under `.sysdoc/cache/textos/documentos/` and `.sysdoc/cache/textos/referencias/`.
- Added `sysdoc create [pasta] [tipo]` deterministic DOCX generation in commit `ca4db6f`.
- Updated README, AGENTS, canonical skill, changelog, package metadata, and CLI tests in commit `ca4db6f`.
- Added additional uncommitted create/TR logic in `sysdoc.py`:
  - `load_analysis_json`
  - `ensure_etp_text`
  - `is_omission_marker`
  - `applicable_etp_items`
  - `apply_etp_revisions`
  - `build_pending_text`
  - `resolve_next_docx_output`
  - procurement category detection
  - reference template selection
- Added additional uncommitted tests in `tests/test_cli.py`:
  - ETP revision replacement
  - pending substitution reporting
  - omission marker handling
  - missing JSON failure
  - auto-prepare for missing ETP cache
  - compras template selection

</completed_work>

<remaining_work>

- Review all uncommitted changes before committing:
  - `AGENTS.md`
  - `CHANGELOG.md`
  - `README.md`
  - `skills/sysdoc/SKILL.md`
  - `sysdoc.py`
  - `tests/test_cli.py`
  - untracked `Referencias/`
- Decide whether `Referencias/` should be ignored, moved into a project folder, or intentionally committed. Do not add it automatically.
- Reconcile implementation against `02-01-PLAN.md`:
  - Original plan expected `python-docx` and `templates/tr_template.docx`.
  - Current implementation uses ZIP/XML placeholder replacement and templates from `referencias/`.
  - Original plan expected output in project root for TR docx.
  - Current general direction introduced `output/`, but uncommitted TR-specific changes currently output `tr_[modelo]_[data].docx` in project root.
- Commit the pending implementation/tests if accepted.
- Create `02-SUMMARY.md` retroactively or run `$gsd-execute-phase 2` formally.
- Only after `02-SUMMARY.md` exists, run `$gsd-add-tests 2`.
</remaining_work>

<decisions_made>

- The user chose `referencias/` instead of `modelos/` because the directory contains broader support material, not only fillable models.
- `documentos/` contains documents to analyze; common case is TR and ETP, but it may contain other documents.
- `prepare` should extract every supported file in both `documentos/` and `referencias/`.
- Normal user workflow should stay simple:
  - `sysdoc prepare`
  - use an AI harness to generate `dados_consolidados.json`
  - `sysdoc render`
  - `sysdoc deploy`
  - `sysdoc create [tipo]` for DOCX generation from JSON/templates
- Avoid `--skill` as a user-facing CLI concept.
- PDF generation is unnecessary for now because Word can save/export PDF.
</decisions_made>

<blockers>

- `gsd-add-tests 2` is blocked by missing `.planning/phases/02-comando-create-word-pdf/02-SUMMARY.md`.
- The GSD state still says Phase 2 is planned and ready for `$gsd-execute-phase 2`, while implementation has already happened manually. This mismatch must be resolved before treating Phase 2 as complete.
- There are uncommitted code and documentation changes. Do not overwrite or revert them.
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-comando-create-word-pdf/02-CONTEXT.md` - original decisions and scope for Phase 2.
2. `.planning/phases/02-comando-create-word-pdf/02-01-PLAN.md` - planned tasks and acceptance criteria.
3. `sysdoc.py` - current implementation, including uncommitted create/TR work.
4. `tests/test_cli.py` - current test coverage, including uncommitted create/TR tests.
5. `README.md`, `AGENTS.md`, `skills/sysdoc/SKILL.md`, `CHANGELOG.md` - user-facing and agent-facing docs changed by the current work.
6. `.planning/STATE.md` - still stale relative to implementation; update after formalizing the phase.

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Manual implementation outside GSD completion artifacts | Phase 2 implementation exists, but no `SUMMARY.md` was created, so downstream workflows such as `$gsd-add-tests` cannot treat it as complete. | advisory | Before running add-tests, create a faithful `02-SUMMARY.md` or execute the phase through `$gsd-execute-phase 2`. |
| Committing user reference material accidentally | `Referencias/` is untracked and appears to contain real DOCX reference models. | advisory | Stage exact files only; explicitly decide what to do with `Referencias/` before any commit. |

## Infrastructure State

- No dev server or watcher is running.
- Last verification run: `python -m pytest tests/ -v` passed with 63 tests.
- `gsd-sdk` was not available earlier in this shell, so this handoff was written directly rather than through `gsd-sdk query`.
- Working tree is dirty by design; see `git status --short`.

## Pre-Execution Critique Required

- Design artifact: `.planning/phases/02-comando-create-word-pdf/02-01-PLAN.md`
- Critique focus:
  - Should Phase 2 keep the newer generic document architecture or return to the narrower original TR-only plan?
  - Should DOCX outputs live in `output/` or root for compatibility with the original plan?
  - Should templates come from `referencias/`, `templates/`, or both?
- Gate: Do not mark Phase 2 complete until these mismatches are resolved in `SUMMARY.md`.

<context>
The immediate goal was to run `$gsd-add-tests 2`, but the workflow correctly refused because Phase 2 is not formally completed. The practical next step is not more test generation; it is formalization of the current implementation state.
</context>

<next_action>
Start with: inspect `git diff --stat`, review `sysdoc.py` and `tests/test_cli.py`, decide what to do with `Referencias/`, then either commit the pending implementation/tests or create a retroactive `02-SUMMARY.md` that accurately states the phase is partially complete and records deviations from the original plan.
</next_action>
